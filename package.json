{
  "name": "workspace",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p public && cp *.html public/ && cp sitemap.xml public/ && cp CNAME public/ && cp -r assets public/",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "serve": "^14.2.5"
  }
}
